---
title: AdUnitStatus
type: enum
---

# `Enum.AdUnitStatus`

Exposes the status of an immersive ad.

Exposes the status of an immersive ad.

The `Enum.AdUnitStatus` enum has 2 item(s).

| Name | Value | Description |
|------|-------|-------------|
| `Enum.AdUnitStatus.Inactive` | 0 | The ad unit isn't currently serving an ad and it may be configured incorrectly. An `Class.AdGui` wit |
| `Enum.AdUnitStatus.Active` | 1 | The ad unit is currently serving an ad. Players will observe sponsored content in an `Class.AdGui` a |
