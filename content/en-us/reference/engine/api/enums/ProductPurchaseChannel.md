---
title: ProductPurchaseChannel
type: enum
---

# `Enum.ProductPurchaseChannel`

Enum which works with `Class.MarketplaceService` to represent how the  
user acquired the developer product.

The `ProductPurchaseChannel` enum works with `Class.MarketplaceService` and
represents how the user acquired the developer product.

The `Enum.ProductPurchaseChannel` enum has 4 item(s).

| Name | Value | Description |
|------|-------|-------------|
| `Enum.ProductPurchaseChannel.InExperience` | 1 | The purchase was made inside of an experience. |
| `Enum.ProductPurchaseChannel.ExperienceDetailsPage` | 2 | The purchase was made outside of an experience, on the **Store** tab of the experience details page. |
| `Enum.ProductPurchaseChannel.AdReward` | 3 | The product was rewarded to the user when they watched a video ad to completion. |
| `Enum.ProductPurchaseChannel.CommerceProduct` | 4 | The user acquired the product as a benefit of purchasing commerce merchandise from the developer of  |
