---
title: InputActionType
type: enum
---

# `Enum.InputActionType`

This enum is used by `Class.InputAction.Type` to determine which input data
type the `Class.InputAction` will receive.

This enum is used by `Class.InputAction.Type` to determine which input data
type the `Class.InputAction` will receive.

The `Enum.InputActionType` enum has 5 item(s).

| Name | Value | Description |
|------|-------|-------------|
| `Enum.InputActionType.Bool` | 0 | The `Class.InputAction` will receive boolean values from button inputs, for example `true`/`false` o |
| `Enum.InputActionType.Direction1D` | 1 | The `Class.InputAction` will receive numerical values, generally from analog gamepad triggers such a |
| `Enum.InputActionType.Direction2D` | 2 | The `Class.InputAction` will receive `Datatype.Vector2` values, generally from thumbstick inputs suc |
| `Enum.InputActionType.Direction3D` | 3 | The `Class.InputAction` will receive `Datatype.Vector3` values from inputs assigned to the `Class.In |
| `Enum.InputActionType.ViewportPosition` | 4 | The `Class.InputAction` will receive `Datatype.Vector2` values representing the absolute pixel (**X* |
