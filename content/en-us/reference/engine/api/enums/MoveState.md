---
title: MoveState
type: enum
---

# `Enum.MoveState`

Represents the state of a `Class.SkateboardPlatform`.

Represents the state of a `Class.SkateboardPlatform`.

The `Enum.MoveState` enum has 5 item(s).

| Name | Value | Description |
|------|-------|-------------|
| `Enum.MoveState.Stopped` | 0 | Skateboard is stopped (not moving). |
| `Enum.MoveState.Coasting` | 1 | Skateboard is coasting (not actively speeding up / being pushed). |
| `Enum.MoveState.Pushing` | 2 | Skateboard is being pushed (speeding up). |
| `Enum.MoveState.Stopping` | 3 | Skateboard is stopping, but still moving. |
| `Enum.MoveState.AirFree` | 4 | Skateboard is in the air. |
