---
title: JointCreationMode
type: enum
---

# `Enum.JointCreationMode`

The way joints are created between two surfaces.

The way joints are created between two surfaces.

The `Enum.JointCreationMode` enum has 3 item(s).

| Name | Value | Description |
|------|-------|-------------|
| `Enum.JointCreationMode.All` | 0 | Joints are created between any kind of surface. Functions identically to Surface. |
| `Enum.JointCreationMode.Surface` | 1 | Joints are created between any kind of surface. Functions identically to All. |
| `Enum.JointCreationMode.None` | 2 | Joints are never created, no matter what. |
