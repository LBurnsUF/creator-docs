---
title: BundleType
type: enum
---

# `Enum.BundleType`

The type of a bundle of assets.

The type of a bundle of assets.

The `Enum.BundleType` enum has 5 item(s).

| Name | Value | Description |
|------|-------|-------------|
| `Enum.BundleType.BodyParts` | 1 | A bundle of body parts and accessories. |
| `Enum.BundleType.Animations` | 2 | A bundle of just animations. |
| `Enum.BundleType.Shoes` | 3 | A bundle of left shoe and right shoe. |
| `Enum.BundleType.DynamicHead` | 4 | A bundle consisting of dynamicHead and moodAnimation assets, optionally with eyebrowAccessory and ey |
| `Enum.BundleType.DynamicHeadAvatar` | 5 |  |
