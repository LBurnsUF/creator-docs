---
title: ControlMode
type: enum
---

# `Enum.ControlMode`

The ControlMode Enum sets how the player is controlled.

The ControlMode Enum sets how the player is controlled.

Note that shift-lock related APIs are in the process of being deprecated, so
it's recommended to use `Class.UserInputService.MouseBehavior` instead to lock
the mouse.

The `Enum.ControlMode` enum has 2 item(s).

| Name | Value | Description |
|------|-------|-------------|
| `Enum.ControlMode.Classic` | 0 | Allows the camera to be moved by clicking and dragging with the right mouse button. |
| `Enum.ControlMode.MouseLockSwitch` | 1 | Similar to classic, but allows the player to toggle mouse locking, causing the camera to rotate as t |
