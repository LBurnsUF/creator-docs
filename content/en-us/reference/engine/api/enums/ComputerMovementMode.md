---
title: ComputerMovementMode
type: enum
---

# `Enum.ComputerMovementMode`

The computer movement type in-use by the client.

The computer movement type in-use by the client.

The `Enum.ComputerMovementMode` enum has 3 item(s).

| Name | Value | Description |
|------|-------|-------------|
| `Enum.ComputerMovementMode.Default` | 0 | The default is KeyboardMouse. |
| `Enum.ComputerMovementMode.KeyboardMouse` | 1 | The player's character is controlled using the keyboard and mouse. |
| `Enum.ComputerMovementMode.ClickToMove` | 2 | The player can right-click in the game world and their character will move there. In addition, the p |
