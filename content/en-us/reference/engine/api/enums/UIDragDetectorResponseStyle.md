---
title: UIDragDetectorResponseStyle
type: enum
---

# `Enum.UIDragDetectorResponseStyle`

Describes how the clicked `Class.GuiObject` will be treated once the desired
motion has been calculated.

Describes how the clicked `Class.GuiObject` will be treated once the desired
motion has been calculated.

The `Enum.UIDragDetectorResponseStyle` enum has 4 item(s).

| Name | Value | Description |
|------|-------|-------------|
| `Enum.UIDragDetectorResponseStyle.Offset` | 0 | Move by the `Datatype.UDim.Offset/Offset` values of the detector's parent's `Class.GuiObject.Positio |
| `Enum.UIDragDetectorResponseStyle.Scale` | 1 | Move by the `Datatype.UDim.Scale/Scale` values of the detector's parent's `Class.GuiObject.Position` |
| `Enum.UIDragDetectorResponseStyle.CustomOffset` | 2 | The UI element will not move at all, but the `Datatype.UDim.Offset/Offset` values of the detector's  |
| `Enum.UIDragDetectorResponseStyle.CustomScale` | 3 | The UI element will not move at all, but the `Datatype.UDim.Scale/Scale` values of the detector's `C |
