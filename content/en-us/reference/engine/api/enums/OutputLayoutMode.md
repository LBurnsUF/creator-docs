---
title: OutputLayoutMode
type: enum
---

# `Enum.OutputLayoutMode`

Sets the layout mode of the output.

Sets the layout mode of the output.

The `Enum.OutputLayoutMode` enum has 2 item(s).

| Name | Value | Description |
|------|-------|-------------|
| `Enum.OutputLayoutMode.Horizontal` | 0 | The output layout is horizontal (x-axis). |
| `Enum.OutputLayoutMode.Vertical` | 1 | The output layout is vertical (y-axis). |
