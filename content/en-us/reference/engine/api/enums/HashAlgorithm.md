---
title: HashAlgorithm
type: enum
---

# `Enum.HashAlgorithm`

A Cryptographic hash function to use in `Class.EncodingService` methods.

A
[Cryptographic hash function](https://en.wikipedia.org/wiki/Cryptographic_hash_function)
to use in `Class.EncodingService` methods.

The `Enum.HashAlgorithm` enum has 5 item(s).

| Name | Value | Description |
|------|-------|-------------|
| `Enum.HashAlgorithm.Blake2b` | 0 | Used to compute a 256-bit (32-byte) hash digest using the [BLAKE2b](https://en.wikipedia.org/wiki/BL |
| `Enum.HashAlgorithm.Blake3` | 1 | Used to compute a 256-bit (32-byte) hash digest using the [BLAKE3](https://en.wikipedia.org/wiki/BLA |
| `Enum.HashAlgorithm.Md5` | 2 | Used to compute a 128-bit (16-byte) hash digest using the [MD5](https://en.wikipedia.org/wiki/MD5) a |
| `Enum.HashAlgorithm.Sha1` | 3 | Used to compute a 160-bit (20-byte) hash digest using the [SHA-1](https://en.wikipedia.org/wiki/SHA- |
| `Enum.HashAlgorithm.Sha256` | 4 | Used to compute a 256-bit (32-byte) hash digest using the [SHA256](https://en.wikipedia.org/wiki/SHA |
