---
title: Button
type: enum
---

# `Enum.Button`

One of the buttons used by `Class.Controller`.

Used for accessing buttons for use by the `Class.Controller`.

The `Enum.Button` enum has 2 item(s).

| Name | Value | Description |
|------|-------|-------------|
| `Enum.Button.Dismount` | 8 | The Dismount button. |
| `Enum.Button.Jump` | 32 | The Jump button. |
