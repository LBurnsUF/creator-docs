---
title: AdShape
type: enum
---

# `Enum.AdShape`

Describes the form factor of an ad in an `Class.AdGui`. There is currently
only one available size.

Describes the form factor of an ad in an `Class.AdGui`. There is currently
only one available size.

The `Enum.AdShape` enum has 1 item(s).

| Name | Value | Description |
|------|-------|-------------|
| `Enum.AdShape.HorizontalRectangle` | 1 | The ad has an aspect ratio of 16:9 and a minimum size of 8 by 4.5 studs. |
