---
title: ScrollingDirection
type: enum
---

# `Enum.ScrollingDirection`

This enum is used by `Class.ScrollingFrame.ScrollingDirection` to specify the
direction(s) in which scrolling is allowed.

This enum is used by `Class.ScrollingFrame.ScrollingDirection` to specify the
direction(s) in which scrolling is allowed.

The `Enum.ScrollingDirection` enum has 3 item(s).

| Name | Value | Description |
|------|-------|-------------|
| `Enum.ScrollingDirection.X` | 1 | The canvas can only be scrolled along the **X** axis. |
| `Enum.ScrollingDirection.Y` | 2 | The canvas can only be scrolled along the **Y** axis. |
| `Enum.ScrollingDirection.XY` | 4 | The canvas can be scrolled along both the **X** and **Y** axes. |
