---
title: VRTouchpadMode
type: enum
---

# `Enum.VRTouchpadMode`

Used to identify the behavior of a specified VR touchpad.

Used to identify the behavior of a specified VR touchpad.

The `Enum.VRTouchpadMode` enum has 3 item(s).

| Name | Value | Description |
|------|-------|-------------|
| `Enum.VRTouchpadMode.Touch` | 0 | The touchpad is treated as ButtonB if it is the left touchpad, or ButtonA if it is the right touchpa |
| `Enum.VRTouchpadMode.VirtualThumbstick` | 1 | The touchpad will function as a gamepad joystick. |
| `Enum.VRTouchpadMode.ABXY` | 2 | The touchpad will function as a means of inputting the standard ABXY gamepad input, dividing it up i |
