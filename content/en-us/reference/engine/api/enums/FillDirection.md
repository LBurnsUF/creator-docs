---
title: FillDirection
type: enum
---

# `Enum.FillDirection`

Used by `Class.UIGridStyleLayout.FillDirection` to control which direction
elements are placed.

Used by `Class.UIGridStyleLayout.FillDirection` to control which direction
elements are placed.

The `Enum.FillDirection` enum has 2 item(s).

| Name | Value | Description |
|------|-------|-------------|
| `Enum.FillDirection.Horizontal` | 0 | Fill in the left-to-right direction. |
| `Enum.FillDirection.Vertical` | 1 | Fill in the top-to-bottom direction. |
