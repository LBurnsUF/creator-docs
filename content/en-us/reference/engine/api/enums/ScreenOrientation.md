---
title: ScreenOrientation
type: enum
---

# `Enum.ScreenOrientation`

The preference of in-game screen orientation on hand-held devices.

Indicates what orientation the game prefers to be played on mobile devices.

The `Enum.ScreenOrientation` enum has 5 item(s).

| Name | Value | Description |
|------|-------|-------------|
| `Enum.ScreenOrientation.LandscapeLeft` | 0 | A preference where the game is displayed landscape (horizontally) with the bottom of the device on t |
| `Enum.ScreenOrientation.LandscapeRight` | 1 | A preference where the game is displayed landscape (horizontally) with the bottom of the device on t |
| `Enum.ScreenOrientation.LandscapeSensor` | 2 | A preference where the game is displayed landscape (horizontally) with the bottom of the device on t |
| `Enum.ScreenOrientation.Portrait` | 3 | A preference where the game is displayed portrait (vertically) with the bottom of the device on the  |
| `Enum.ScreenOrientation.Sensor` | 4 | A preference where the game is displayed depending on the best match to the device's current orienta |
