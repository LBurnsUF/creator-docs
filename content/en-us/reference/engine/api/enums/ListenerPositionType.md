---
title: ListenerPositionType
type: enum
---

# `Enum.ListenerPositionType`

Defines where and how an `Class.AudioListener` is positioned when receiving
spatial audio.

Defines where and how an `Class.AudioListener` is positioned when receiving
spatial audio.

The `Enum.ListenerPositionType` enum has 2 item(s).

| Name | Value | Description |
|------|-------|-------------|
| `Enum.ListenerPositionType.Parent` | 0 | Uses the parent `Class.Instance` of the `Class.AudioEmitter`. |
| `Enum.ListenerPositionType.Instance` | 1 | Uses a specified `Class.Instance`. |
