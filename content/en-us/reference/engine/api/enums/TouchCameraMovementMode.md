---
title: TouchCameraMovementMode
type: enum
---

# `Enum.TouchCameraMovementMode`

Changes the touch camera movement mode currently in-use by the client using a
TouchEnabled device.

Changes the touch camera movement mode currently in-use by the client using a
TouchEnabled device.

The `Enum.TouchCameraMovementMode` enum has 4 item(s).

| Name | Value | Description |
|------|-------|-------------|
| `Enum.TouchCameraMovementMode.Default` | 0 | Default mode used by Roblox core scripts. |
| `Enum.TouchCameraMovementMode.Classic` | 1 | Default mode used by Roblox core scripts. |
| `Enum.TouchCameraMovementMode.Follow` | 2 | Camera moves with the subject and rotates to keep the subject in the center. |
| `Enum.TouchCameraMovementMode.Orbital` | 3 | The camera locks at a certain angle. Once it's set, players can rotate the camera, but it will alway |
