---
title: PredictionMode
type: enum
---

# `Enum.PredictionMode`

Enum used with `Class.RunService:SetPredictionMode()` to define the prediction
mode for the instance.

Enum used with `Class.RunService:SetPredictionMode()` to define the prediction
mode for the instance.

The `Enum.PredictionMode` enum has 3 item(s).

| Name | Value | Description |
|------|-------|-------------|
| `Enum.PredictionMode.Automatic` | 0 | The engine will automatically decide if this instance should be predicted or not. Currently, only `C |
| `Enum.PredictionMode.On` | 1 | The instance will be predicted. Mismatches in the attributes of this instance between the client and |
| `Enum.PredictionMode.Off` | 2 | The instance will **not** be predicted. Regular network ownership semantics apply. |
