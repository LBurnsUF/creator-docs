---
title: StepFrequency
type: enum
---

# `Enum.StepFrequency`

Enum used with `Class.RunService:BindToSimulation()` to indicate the frequency
at which the bound function is called.

Enum used with `Class.RunService:BindToSimulation()` to indicate the frequency
at which the bound function is called.

The `Enum.StepFrequency` enum has 6 item(s).

| Name | Value | Description |
|------|-------|-------------|
| `Enum.StepFrequency.Hz60` | 0 | The bound function is called at 60 Hz. |
| `Enum.StepFrequency.Hz30` | 1 | The bound function is called at 30 Hz. |
| `Enum.StepFrequency.Hz15` | 2 | The bound function is called at 15 Hz. |
| `Enum.StepFrequency.Hz10` | 3 | The bound function is called at 10 Hz. |
| `Enum.StepFrequency.Hz5` | 4 | The bound function is called at 5 Hz. |
| `Enum.StepFrequency.Hz1` | 5 | The bound function is called at 1 Hz. |
