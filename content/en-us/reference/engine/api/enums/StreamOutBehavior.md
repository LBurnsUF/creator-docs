---
title: StreamOutBehavior
type: enum
---

# `Enum.StreamOutBehavior`

Determines how content is streamed out from `Class.Player` clients.

Determines how content is streamed out from `Class.Player` clients.

The `Enum.StreamOutBehavior` enum has 3 item(s).

| Name | Value | Description |
|------|-------|-------------|
| `Enum.StreamOutBehavior.Default` | 0 | Default behavior (subject to change). |
| `Enum.StreamOutBehavior.LowMemory` | 1 | Only stream out when memory is low. |
| `Enum.StreamOutBehavior.Opportunistic` | 2 | Stream out content that is significantly outside of the current `Class.Workspace.StreamingTargetRadi |
