---
title: HighlightDepthMode
type: enum
---

# `Enum.HighlightDepthMode`

Controls how the `Class.Highlight` effect displays with respect to other
objects in the world.

Controls how the `Class.Highlight` effect displays with respect to other
objects in the world.

The `Enum.HighlightDepthMode` enum has 2 item(s).

| Name | Value | Description |
|------|-------|-------------|
| `Enum.HighlightDepthMode.AlwaysOnTop` | 0 | Allows the `Class.Highlight` to display regardless if there are objects between the camera and the h |
| `Enum.HighlightDepthMode.Occluded` | 1 | Hides the `Class.Highlight` if there are objects between the camera and the highlighted object. This |
