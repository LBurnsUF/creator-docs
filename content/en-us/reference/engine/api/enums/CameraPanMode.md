---
title: CameraPanMode
type: enum
---

# `Enum.CameraPanMode`

The CameraPanMode Enum represents the available pan modes for
`Class.Camera:SetCameraPanMode()`.

The CameraPanMode Enum represents the available pan modes for
`Class.Camera:SetCameraPanMode()`.

The `Enum.CameraPanMode` enum has 2 item(s).

| Name | Value | Description |
|------|-------|-------------|
| `Enum.CameraPanMode.Classic` | 0 | Enables swipe and pan on mobile devices, and disables edge bump camera controls. Default. |
| `Enum.CameraPanMode.EdgeBump` | 1 | Disables swipe to pan on mobile devices, and enables edge bump camera controls. It does not affect W |
