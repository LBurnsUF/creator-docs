---
title: OrientationAlignmentMode
type: enum
---

# `Enum.OrientationAlignmentMode`

The OrientationAlignmentMode Enum is used to select the number of
`Class.Attachment|Attachments` used in an alignment.

The OrientationAlignmentMode Enum is used to select the number of
`Class.Attachment|Attachments` used in an alignment.

The `Enum.OrientationAlignmentMode` enum has 2 item(s).

| Name | Value | Description |
|------|-------|-------------|
| `Enum.OrientationAlignmentMode.OneAttachment` | 0 | Use one attachment. |
| `Enum.OrientationAlignmentMode.TwoAttachment` | 1 | Use two attachments. |
