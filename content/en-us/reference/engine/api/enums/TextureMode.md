---
title: TextureMode
type: enum
---

# `Enum.TextureMode`

Describes how the texture of a `Class.Trail` or `Class.Beam` behaves.

Describes how the texture of a `Class.Trail` or `Class.Beam` behaves.

The `Enum.TextureMode` enum has 3 item(s).

| Name | Value | Description |
|------|-------|-------------|
| `Enum.TextureMode.Stretch` | 0 | For a `Class.Trail`, the texture will stretch out based on the lifetime of the trail, and shrink inw |
| `Enum.TextureMode.Wrap` | 1 | For a `Class.Trail`, the texture will be tiled as the length of the trail changes, but the textures  |
| `Enum.TextureMode.Static` | 2 | For a `Class.Trail`, the texture will be rolled out as the attachments move, and they will remain in |
