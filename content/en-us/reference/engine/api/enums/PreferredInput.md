---
title: PreferredInput
type: enum
---

# `Enum.PreferredInput`

This enum is used with `Class.UserInputService.PreferredInput` to indicate the
primary input type a player is likely using.

This enum is used with `Class.UserInputService.PreferredInput` to indicate the
primary input type a player is likely using.

The `Enum.PreferredInput` enum has 3 item(s).

| Name | Value | Description |
|------|-------|-------------|
| `Enum.PreferredInput.KeyboardAndMouse` | 0 | The player has connected or most recently interacted with a keyboard or mouse. |
| `Enum.PreferredInput.Gamepad` | 1 | The player has connected or most recently interacted with a gamepad. |
| `Enum.PreferredInput.Touch` | 2 | The player's device has touch capability and no other input method is available or was recently inte |
