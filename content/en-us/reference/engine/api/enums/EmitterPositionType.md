---
title: EmitterPositionType
type: enum
---

# `Enum.EmitterPositionType`

Defines where and how an `Class.AudioEmitter` is positioned when sending out
spatial audio.

Defines where and how an `Class.AudioEmitter` is positioned when sending out
spatial audio.

The `Enum.EmitterPositionType` enum has 2 item(s).

| Name | Value | Description |
|------|-------|-------------|
| `Enum.EmitterPositionType.Parent` | 0 | Uses the parent `Class.Instance` of the `Class.AudioEmitter`. |
| `Enum.EmitterPositionType.Instance` | 1 | Uses a specified `Class.Instance`. |
