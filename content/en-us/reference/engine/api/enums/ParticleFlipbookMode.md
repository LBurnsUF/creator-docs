---
title: ParticleFlipbookMode
type: enum
---

# `Enum.ParticleFlipbookMode`

Determines the type of the flipbook animation.

Determines the type of the flipbook animation.

The `Enum.ParticleFlipbookMode` enum has 4 item(s).

| Name | Value | Description |
|------|-------|-------------|
| `Enum.ParticleFlipbookMode.Loop` | 0 | Continuously play through all frames, starting back at the first frame after playing the last. |
| `Enum.ParticleFlipbookMode.OneShot` | 1 | Play through the animation only once across the particle's lifetime. With this setting, the `Class.P |
| `Enum.ParticleFlipbookMode.PingPong` | 2 | Play from the first to the last frame, then in reverse from the last to the first, repeating through |
| `Enum.ParticleFlipbookMode.Random` | 3 | Play the frames in a random order, blending/crossfading from one frame to the next. This can be usef |
