---
title: ReverbType
type: enum
---

# `Enum.ReverbType`

The ReverbType Enum allows you to make audio in your game sound different,
depending on what "area" the sounds are in.

The ReverbType Enum allows you to make audio in your game sound different,
depending on what "area" the sounds are in.

Note that some of these do not appear to have any effect on the sound.

The `Enum.ReverbType` enum has 24 item(s).

| Name | Value | Description |
|------|-------|-------------|
| `Enum.ReverbType.NoReverb` | 0 | No sound reverb. Audio is not changed from default. |
| `Enum.ReverbType.GenericReverb` | 1 | Sound reverb is changed to a generic reverb effect. |
| `Enum.ReverbType.PaddedCell` | 2 | Sound reverb is changed to sound like the player is in a padded cell. |
| `Enum.ReverbType.Room` | 3 | Sound reverb is changed to sound like the player is in a room. |
| `Enum.ReverbType.Bathroom` | 4 | Sound reverb is changed to sound like the player is in a bathroom. |
| `Enum.ReverbType.LivingRoom` | 5 | Sound reverb is changed to sound like the player is in a living room. |
| `Enum.ReverbType.StoneRoom` | 6 | Sound reverb is changed to sound like the player is in a stone room. |
| `Enum.ReverbType.Auditorium` | 7 | Sound reverb is changed to sound like the player is in an auditorium. |
| `Enum.ReverbType.ConcertHall` | 8 | Sound reverb is changed to sound like the player is in a concert hall. |
| `Enum.ReverbType.Cave` | 9 | Sound reverb is changed to sound like the player is in a cave. |
| `Enum.ReverbType.Arena` | 10 | Sound reverb is changed to sound like the player is in an arena. |
| `Enum.ReverbType.Hangar` | 11 | Sound reverb is changed to sound like the player is in a hangar. |
| `Enum.ReverbType.CarpettedHallway` | 12 | Sound reverb is changed to sound like the player is in a carpeted hallway. |
| `Enum.ReverbType.Hallway` | 13 | Sound reverb is changed to sound like the player is in a hallway. |
| `Enum.ReverbType.StoneCorridor` | 14 | Sound reverb is changed to sound like the player is in a stone corridor. |
| `Enum.ReverbType.Alley` | 15 | Sound reverb is changed to sound like the player is in an alley. |
| `Enum.ReverbType.Forest` | 16 | Sound reverb is changed to sound like the player is in a forest. |
| `Enum.ReverbType.City` | 17 | Sound reverb is changed to sound like the player is in a city. |
| `Enum.ReverbType.Mountains` | 18 | Sound reverb is changed to sound like the player is in the mountains. |
| `Enum.ReverbType.Quarry` | 19 | Sound reverb is changed to sound like the player is in a quarry. |
| `Enum.ReverbType.Plain` | 20 | Sound reverb is changed to sound like the player is an open plain. |
| `Enum.ReverbType.ParkingLot` | 21 | Sound reverb is changed to sound like the player is in a parking lot. |
| `Enum.ReverbType.SewerPipe` | 22 | Sound reverb is changed to sound like the player is in a sewer pipe. |
| `Enum.ReverbType.UnderWater` | 23 | Sound reverb is changed to sound like the player is underwater. |
