---
title: FrameStyle
type: enum
---

# `Enum.FrameStyle`

Used to set the style of a `Class.Frame`.

The **FrameStyle** enum is used to set the style of a `Class.Frame`.

The `Enum.FrameStyle` enum has 7 item(s).

| Name | Value | Description |
|------|-------|-------------|
| `Enum.FrameStyle.Custom` | 0 | Uses the frame's `Class.GuiObject.BackgroundColor3`, `Class.GuiObject.BorderColor3`, and `Class.GuiO |
| `Enum.FrameStyle.ChatBlue` | 1 | Causes the frame to appear similar to a `Class.Dialog` with its `Class.Dialog.Tone/Tone` property se |
| `Enum.FrameStyle.RobloxSquare` | 2 | Causes the frame to appear as a translucent dark gray rectangle with a padding of five pixels on all |
| `Enum.FrameStyle.RobloxRound` | 3 | Causes the frame to appear as a translucent dark gray rectangle with rounded edges. Like **RobloxSqu |
| `Enum.FrameStyle.ChatGreen` | 4 | Causes the frame to appear similar to a `Class.Dialog` with its `Class.Dialog.Tone/Tone` property se |
| `Enum.FrameStyle.ChatRed` | 5 | Causes the frame to appear similar to a `Class.Dialog` with its `Class.Dialog.Tone/Tone` property se |
| `Enum.FrameStyle.DropShadow` | 6 | Causes the frame to appear as a translucent gray rectangle with blurred sides. The blur is more appa |
