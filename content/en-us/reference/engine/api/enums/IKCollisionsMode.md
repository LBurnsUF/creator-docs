---
title: IKCollisionsMode
type: enum
---

# `Enum.IKCollisionsMode`

The `Enum.IKCollisionsMode` enum has 3 item(s).

| Name | Value | Description |
|------|-------|-------------|
| `Enum.IKCollisionsMode.NoCollisions` | 0 | Only the part and any parts directly joined to it via joints/`Class.Constraint/constraints` be invol |
| `Enum.IKCollisionsMode.OtherMechanismsAnchored` | 1 | Only the part and any parts directly jointed to it via joints/`Class.Constraint/constraints` will be |
| `Enum.IKCollisionsMode.IncludeContactedMechanisms` | 2 | The part, any parts directly joined to it via joints/`Class.Constraint/constraints`, and any parts w |
