---
title: ParticleFlipbookLayout
type: enum
---

# `Enum.ParticleFlipbookLayout`

Determines the layout of the flipbook texture.

Determines the layout of the flipbook texture.

The `Enum.ParticleFlipbookLayout` enum has 5 item(s).

| Name | Value | Description |
|------|-------|-------------|
| `Enum.ParticleFlipbookLayout.None` | 0 | Disable flipbook features and use the texture as a single static texture over the particle's lifetim |
| `Enum.ParticleFlipbookLayout.Grid2x2` | 1 | 2&times;2 frames for a 4-frame animation. |
| `Enum.ParticleFlipbookLayout.Grid4x4` | 2 | 4&times;4 frames for a 16-frame animation. |
| `Enum.ParticleFlipbookLayout.Grid8x8` | 3 | 8&times;8 frames for a 64-frame animation. |
| `Enum.ParticleFlipbookLayout.Custom` | 4 | A custom grid size defined by `Class.ParticleEmitter.FlipbookSizeX/FlipbookSizeX` and `Class.Particl |
