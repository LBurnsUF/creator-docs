---
title: FilterResult
type: enum
---

# `Enum.FilterResult`

Used to determine the result of a text filter request.

Used to determine the result of a text filter request.

The `Enum.FilterResult` enum has 2 item(s).

| Name | Value | Description |
|------|-------|-------------|
| `Enum.FilterResult.Accepted` | 0 | Text accepted by filter. |
| `Enum.FilterResult.Rejected` | 1 | Text rejected by filter. |
