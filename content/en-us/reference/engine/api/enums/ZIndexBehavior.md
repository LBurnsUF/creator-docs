---
title: ZIndexBehavior
type: enum
---

# `Enum.ZIndexBehavior`

Used by `Class.LayerCollector.ZIndexBehavior` to control how the
`Class.GuiObject.ZIndex` property behaves for descendants.

Used by `Class.LayerCollector.ZIndexBehavior` to control how the
`Class.GuiObject.ZIndex` property behaves for descendants.

The `Enum.ZIndexBehavior` enum has 2 item(s).

| Name | Value | Description |
|------|-------|-------------|
| `Enum.ZIndexBehavior.Global` | 0 | The ZIndex property overrides the default value computed from the depth in the hierarchy. |
| `Enum.ZIndexBehavior.Sibling` | 1 | The ZIndex property controls the order in which the GuiObject renders relative to its siblings. |
