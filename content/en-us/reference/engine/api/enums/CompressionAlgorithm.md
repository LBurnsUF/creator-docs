---
title: CompressionAlgorithm
type: enum
---

# `Enum.CompressionAlgorithm`

A compression algorithm to use in `Class.EncodingService` methods.

A compression algorithm to use in `Class.EncodingService` methods.

The `Enum.CompressionAlgorithm` enum has 1 item(s).

| Name | Value | Description |
|------|-------|-------------|
| `Enum.CompressionAlgorithm.Zstd` | 0 | Used to perform compression using [Zstandard](https://en.wikipedia.org/wiki/Zstd) compression, also  |
