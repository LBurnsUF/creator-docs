---
title: TextTruncate
type: enum
---

# `Enum.TextTruncate`

Controls the truncation of text when using the `TextTruncate` property.

Controls the truncation of text when using the `TextTruncate` property.

The `Enum.TextTruncate` enum has 3 item(s).

| Name | Value | Description |
|------|-------|-------------|
| `Enum.TextTruncate.None` | 0 | Text is not truncated. |
| `Enum.TextTruncate.AtEnd` | 1 | Text is truncated at the end of the text; extra graphemes that cannot fit into the space are replace |
| `Enum.TextTruncate.SplitWord` | 2 | If the end of the line occurs in the middle of a word, the text is truncated inside of that word. Ex |
