---
title: VerticalAlignment
type: enum
---

# `Enum.VerticalAlignment`

Used by `Class.UIGridStyleLayout.VerticalAlignment` to align the layout
vertically within its parent.

Used by `Class.UIGridStyleLayout.VerticalAlignment` to align the layout
vertically within its parent.

The `Enum.VerticalAlignment` enum has 3 item(s).

| Name | Value | Description |
|------|-------|-------------|
| `Enum.VerticalAlignment.Center` | 0 | Grid is aligned to the center of its parent along the **Y** axis. |
| `Enum.VerticalAlignment.Top` | 1 | Grid is aligned to the top edge of its parent. |
| `Enum.VerticalAlignment.Bottom` | 2 | Grid is aligned to the bottom edge of its parent. |
