---
title: HapticEffectType
type: enum
---

# `Enum.HapticEffectType`

Enum used alongside `Class.HapticEffect.Type`.

Enum used alongside `Class.HapticEffect.Type`.

The `Enum.HapticEffectType` enum has 6 item(s).

| Name | Value | Description |
|------|-------|-------------|
| `Enum.HapticEffectType.Custom` | 0 | Allows for application of a custom haptic waveform through the `Class.HapticEffect:SetWaveformKeys() |
| `Enum.HapticEffectType.UIHover` | 1 | Useful for when a player browses over an object (often a UI object) without the intention of trigger |
| `Enum.HapticEffectType.UIClick` | 2 | Useful for when a player has selected an object (often a UI object) with the intention of triggering |
| `Enum.HapticEffectType.UINotification` | 3 | Useful for when there is an inbound message that should draw the player's attention away from their  |
| `Enum.HapticEffectType.GameplayExplosion` | 4 | Useful to signify a large-scale physics event that triggers impact across a large portion of a given |
| `Enum.HapticEffectType.GameplayCollision` | 5 | This effect is a large immediate rumble that dies down quickly, useful to signify a clear and purpos |
