---
title: SurfaceGuiSizingMode
type: enum
---

# `Enum.SurfaceGuiSizingMode`

Used by `Class.SurfaceGui.SizingMode` to control the sizing behavior of a
`Class.SurfaceGui`.

Used by `Class.SurfaceGui.SizingMode` to control the sizing behavior of a
`Class.SurfaceGui`.

The `Enum.SurfaceGuiSizingMode` enum has 2 item(s).

| Name | Value | Description |
|------|-------|-------------|
| `Enum.SurfaceGuiSizingMode.FixedSize` | 0 | Renders with a fixed size set using `Class.SurfaceGui.CanvasSize`. |
| `Enum.SurfaceGuiSizingMode.PixelsPerStud` | 1 | Renders with a variable size based on `Class.SurfaceGui.PixelsPerStud` and the SurfaceGui's size in  |
