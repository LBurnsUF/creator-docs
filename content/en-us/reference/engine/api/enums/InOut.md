---
title: InOut
type: enum
---

# `Enum.InOut`

The InOut Enum is used to set where the object is on the side of its parent.

The InOut Enum is used to set where the object is on the side of its parent.

The `Enum.InOut` enum has 3 item(s).

| Name | Value | Description |
|------|-------|-------------|
| `Enum.InOut.Edge` | 0 | The object is located along an edge of its parent. |
| `Enum.InOut.Inset` | 1 | The object's located is inset from its parent's location. |
| `Enum.InOut.Center` | 2 | The object's location is centered on it's parent. |
