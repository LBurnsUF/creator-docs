---
title: HorizontalAlignment
type: enum
---

# `Enum.HorizontalAlignment`

Used by `Class.UIGridStyleLayout.HorizontalAlignment` to align the layout
horizontally within its parent.

Used by `Class.UIGridStyleLayout.HorizontalAlignment` to align the layout
horizontally within its parent.

The `Enum.HorizontalAlignment` enum has 3 item(s).

| Name | Value | Description |
|------|-------|-------------|
| `Enum.HorizontalAlignment.Center` | 0 | Grid is aligned to the center of its parent along the **X** axis. |
| `Enum.HorizontalAlignment.Left` | 1 | Grid is aligned to the left edge of its parent. |
| `Enum.HorizontalAlignment.Right` | 2 | Grid is aligned to the right edge of its parent. |
