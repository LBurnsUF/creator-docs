---
title: SurfaceConstraint
type: enum
---

# `Enum.SurfaceConstraint`

This item is deprecated and is replaced by the `Enum.SurfaceType` enum.

This item is deprecated and is replaced by the `Enum.SurfaceType` enum.

The `Enum.SurfaceConstraint` enum has 4 item(s).

| Name | Value | Description |
|------|-------|-------------|
| `Enum.SurfaceConstraint.None` | 0 | No surface constraint. |
| `Enum.SurfaceConstraint.Hinge` | 1 | Makes the side appear with a yellow hinge. Any part connected to this hinge will stick to the side a |
| `Enum.SurfaceConstraint.SteppingMotor` | 2 | Functions identically to a motor. It may have functioned differently in the past, but that functiona |
| `Enum.SurfaceConstraint.Motor` | 3 | Acts the same as a Hinge, but has a grey ring around it and automatically rotates any part connected |
