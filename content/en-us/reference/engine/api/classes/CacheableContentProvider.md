---
title: CacheableContentProvider
type: class
superclass: Instance
tags: [NotCreatable, Service, NotReplicated]
---

# CacheableContentProvider

A variant of the `Class.ContentProvider` that caches assets that have already
been received. This service is not used directly, but it is used by the
services that inherit from it.

**Inherits from:** `Class.Instance` > `Class.Object`

**Tags:** [NotCreatable] [Service] [NotReplicated]

## Description

A variant of the `Class.ContentProvider` that caches assets that have already
been received. This service is not used directly, but it is used by the
services that inherit from it.
