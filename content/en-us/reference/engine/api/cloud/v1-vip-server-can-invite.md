---
title: V1 / Vip Server / Can Invite
type: cloud-api
tags: [Private servers, Users]
---

# V1 / Vip Server / Can Invite

Cloud API resource group with 1 endpoint(s).

## Endpoints

| Method | Path | Description |
|--------|------|-------------|
| `GET` | `/v1/vip-server/can-invite/{userId}` |  |

### `GET` `/v1/vip-server/can-invite/{userId}`

**Operation:** `PrivateServers_CanInviteUser`

**Parameters:**

- `userId` (path, integer (required)) - 

**Responses:**

- `200` - Success
